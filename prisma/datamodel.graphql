type User {
  id: ID! @unique
  name: String!
}

type Task {
  id: ID! @unique
  type: TaskType!
  title: String!
}

type Exercise {
  id: ID! @unique
  task: Task!
  answer: String!
}

enum TaskType {
  CORRECT_ORDER_OF_WORDS
  WHICH_IS_CORRECT
}
